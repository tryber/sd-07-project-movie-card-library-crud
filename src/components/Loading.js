import React, { Component } from 'react';
import '../styles/components/Loading.css';

class Loading extends Component {
  render() {
    return (
      <div className="loading-element">Carregando...</div>
    );
  }
}

export default Loading;
